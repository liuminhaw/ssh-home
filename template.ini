[DEFAULT]
sheet-id = 'Set sheet id to read here'
credential-file = credentials.json
template-sheet = Template

[CUSTOM]
credential-file = 'Set custom credential file path here if needed (Optional)'
template-sheet = 'Set custom template sheet name here if needed (Optional)'